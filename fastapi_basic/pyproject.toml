[project]
name = "fastapi-basic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hatholion", email = "hatholion@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.13.5",
    "pydentic>=0.0.1.dev3",
    "uvicorn>=0.53.0",
]

[project.scripts]
fastapi-basic = "fastapi_basic:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
